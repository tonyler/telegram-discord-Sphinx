╔════════════════════════════════════════════════════════════════╗
║          SPHINX BINDING - QUICK START GUIDE                    ║
╚════════════════════════════════════════════════════════════════╝

📍 LOCAL DEVELOPMENT (HTTP)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ./start_dev.sh

  → Opens at: http://localhost:8000
  → Perfect for testing before production


🚀 PRODUCTION DEPLOYMENT (HTTPS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  sudo bash setup_https.sh

  You'll need:
    • A domain name (e.g., binding.example.com)
    • Domain pointed to your server IP
    • Email address for SSL certificate

  This ONE command does EVERYTHING:
    ✓ Installs Nginx
    ✓ Gets FREE SSL certificate
    ✓ Sets up auto-start on boot
    ✓ Configures HTTPS redirect
    ✓ Enables auto-renewal


⚙️  AFTER DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  1. Update .env file:
     ENVIRONMENT=production
     BASE_URL=https://yourdomain.com
     DISCORD_REDIRECT_URI=https://yourdomain.com/auth/discord/callback

  2. Restart app:
     sudo systemctl restart binding

  3. Setup Telegram webhook:
     source venv/bin/activate
     python3 setup_telegram_webhook.py

  4. Update Discord OAuth settings:
     https://discord.com/developers/applications
     → Add redirect: https://yourdomain.com/auth/discord/callback


🔧 USEFUL COMMANDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  View logs (live):        sudo journalctl -u binding -f
  Restart app:             sudo systemctl restart binding
  Check status:            sudo systemctl status binding
  Stop app:                sudo systemctl stop binding
  Check SSL certificate:   sudo certbot certificates


📚 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Full guide:       DEPLOYMENT.md
  Project info:     README.md
  Config example:   .env.example


❓ TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  App won't start?
    → sudo journalctl -u binding -n 50

  Can't access website?
    → sudo systemctl status nginx
    → sudo systemctl status binding

  SSL issues?
    → sudo certbot renew --dry-run


══════════════════════════════════════════════════════════════════

  That's it! Everything is automated.
  See DEPLOYMENT.md for detailed documentation.

══════════════════════════════════════════════════════════════════
